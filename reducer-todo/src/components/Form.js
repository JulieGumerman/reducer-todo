import React from "react";

const Form = () => {
    return (
        <div>
            <form>
                <input placeholder="remember to..." />
            </form>
        </div>
    );
}

export default Form;